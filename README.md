# 🎲 营销活动抽奖与积分激励运营平台

基于 **Vue 3 + Vite + Pinia** 的营销运营平台，覆盖抽奖玩法、积分体系与运营管理。

## 功能特性

- **多玩法抽奖**
  - 🎡 幸运转盘（SVG 扇形 + 旋转动画）与 🎰 刮刮乐（Canvas 刮涂层揭晓）
  - 加权随机抽取、奖品库存扣减、积分奖品自动入账
- **抽奖任务自动结算**：任务进度按真实参与记录累计（正常 + 审核放行计入；风控冻结暂缓、撤销不计），达标自动发奖并同步积分流水；风控放行后按记录归属业务日补计、撤销后确认回退；任务进度与领奖记录按业务日归档台账，跨日审核不串账、不重复发奖
- **风控校验**：每日限抽 / 累计限抽 / 奖品库存余量 / 积分不足拦截
- **风控申诉**：抽奖与兑换命中可配置规则时生成风险审核单，冻结相关积分、预占奖品/商品库存；用户可申诉，运营审核后放行（发奖/核销）或撤销（返还积分/库存/次数）；角色权限 + 全量操作记录，状态机保证幂等防重复扣减
- **积分库存对账**：按业务日三方交叉核对（业务台账 / 积分流水 / 库存流水+实时账）抽奖成本与积分奖品、兑换成本、任务奖励、余额、冻结积分、库存消耗与预占，逐项生成带证据的可追溯差异单；运营复核后通过 **append-only 补偿流水**修正积分/库存（原始记录永不改写），支持差异挂账、跨日补核（补偿计入执行日、归属业务日保留不串账）、重复执行幂等收敛；余额、库存、看板与审计实时同步
- **积分体系**：签到与任务赚积分、积分明细流水（含冻结/返还/放行/任务结算/建账/对账补偿标记）、积分商城兑换
- **运营侧**：运营看板（累计抽奖、参与人数、中奖统计、库存、待风控/冻结积分、任务结算量、对账待复核差异/补偿量）、活动管理（新建 / 暂停 / 重置库存 / 删除）
- **演示友好**：内置多套 mock 活动、预置用户积分、预置风控审核单（待审/已申诉/已放行/已撤销/跨日待审）与历史业务日任务台账，纯前端零后端即可浏览运行

## 快速开始

```bash
npm install
npm run dev
# 打开 http://localhost:5174
```

## 项目结构

```
lottery-platform/
├── index.html
├── vite.config.js
├── .gitignore
└── src/
    ├── main.js
    ├── App.vue                 # 布局 + Tab 导航 + Toast
    ├── style.css
    ├── mock/
    │   └── data.js             # 活动/奖品/任务/商城 mock 数据
    ├── store/
    │   └── platform.js         # Pinia：抽奖/积分/兑换/风控审核/管理逻辑
    └── components/
        ├── ActivityView.vue    # 活动详情（奖品池 + 玩法区 + 风控冻结提示）
        ├── LuckWheel.vue       # 幸运转盘动画
        ├── ScratchCard.vue     # 刮刮乐 Canvas 刮卡
        ├── PointsCenter.vue    # 积分任务/商城/流水（冻结标记与审核入口）
        ├── RiskCenter.vue      # 风控申诉：规则配置 + 审核单 + 操作记录
        ├── ReconCenter.vue     # 积分库存对账：对账任务单 + 差异复核 + 补偿流水
        ├── DashboardView.vue   # 运营看板统计（含对账指标）
        └── AdminView.vue       # 活动管理 + 新建表单
```

## 抽奖任务自动结算说明

- **进度来源**：抽奖任务（如「今日抽奖3次」）进度唯一来源于真实参与记录——状态为 `正常` 与 `已放行` 的抽奖记录计入；`冻结` 暂缓计入，`撤销` 不计入。无手动领取入口，杜绝刷任务与重复发奖。
- **自动结算**：每次有效参与落账后按归属业务日结算，达标即自动发放奖励并写入积分流水（`任务结算：xxx`）；同一（任务 × 业务日）仅发奖一次（`taskClaims` 台账判重，重复触发幂等跳过）。
- **风控联动**
  - 冻结：该笔暂缓计入，任务页与审核单均提示"审核中暂缓"；
  - 放行：按参与记录的**归属业务日**补计进度，若达标自动补发奖励（流水与台账标注"跨日补计"），不计入审核当日，避免跨日串账；
  - 撤销：该笔确认不计入（冻结期未计入，撤销后自然回退），积分/库存/限次按既有规则返还。
- **按业务日归档**：积分中心「任务结算记录」按业务日分册展示每日进度、审核中笔数与领奖记录（含归属日/发放日）；业务日切换时先兜底结算上一日再归档，历史台账保留不丢。
- **同步积分流水**：任务奖励实时入账并写 append-only 流水；跨日补计的流水日期为实际发放日、备注归属业务日，保证账实一致。
- 逻辑冒烟测试：`npm run test:settle`（覆盖进度累计/冻结暂缓/放行补计/撤销回退/幂等防重/跨日隔离）。

## 风控申诉说明

- **可配置规则**（「🛡️ 风控申诉」运营视角配置，即时生效）：高价值奖品稀有度、当日抽奖频次、短时连抽/连兑窗口、高价值兑换积分阈值、用户黑名单、风控总开关
- **审核单状态机**：`待审核 pending → 已申诉 appealed → 已放行 released / 已撤销 revoked`，仅待处理态可申诉/审核，重复提交自动拦截
- **冻结语义**
  - 积分：抽奖/兑换成本即时从可用余额转入冻结；放行则核销为消费（积分奖品此时才入账），撤销则原路返还
  - 库存：奖品/商品 `remain -1` 同时 `frozen +1` 预占（不超卖）；放行核销预占，撤销回补
  - 限次：冻结抽奖占用每日/累计次数，撤销后返还
- **权限**：用户仅能对本人单据申诉；仅运营可放行/撤销/配置规则（页内可切换演示视角）
- **审计**：冻结、申诉、放行、撤销、规则变更、视角切换全部写入操作记录；积分流水 append-only，业务记录与看板统计随审核结果同步

## 积分库存对账说明

- **三方交叉核对**（避免"自己证自己"，差异才有意义）
  - 业务台账：抽奖 / 兑换业务记录、风控审核单、任务领奖台账（业务事实）
  - 积分账册：append-only 积分流水，按**归属业务日** `bizDate` 归属（跨日审核的放行/撤销/补偿各记各的账）
  - 库存账册：append-only 库存流水（`init 建账 / hold 预占 / release 核销 / revoke 回补 / reset 重置 / recon 补偿`）+ 实时 `remain / frozen`
- **核对项**
  - 逐笔：抽奖积分成本、抽奖积分奖品、兑换积分成本（业务记录与同笔流水净额配对，撤销单冻结+返还净额为 0）
  - 按日：任务奖励（自动结算台账 vs 任务结算流水）、库存净消耗（业务净出库 vs 当日库存流水）
  - 实时（当前业务日）：积分余额（全部流水含建账行重放 == 实时余额）、冻结积分（待审结单冻结合计 vs 冻结流水）、库存余量守恒（初始库存+流水重放 == `remain`）、预占库存（审核单台账 / 流水重放 / 实时 `frozen` 三向核对）
- **差异单可追溯**：每项差异保留台账应有值、账面实际值、差异额与双方证据；首次快照 `origExpected/origActual` 永不覆盖
- **复核处理**（仅运营）
  - **补偿修正**：写 append-only 补偿流水修正积分余额或库存余量/预占，实时账随之同步；**原始业务记录与历史流水一律不改写**
  - **差异挂账**：确认为合理差异时不改动账目，留痕保留待跟进，不阻塞任务单核平
  - 补偿执行日落当前业务日、归属业务日保留（跨日补核不串当日账），积分流水/库存流水/审计日志全部留痕
- **幂等与跨日**
  - 同一业务日只有一张对账任务单，记录执行次数与末次时间；重复执行复用原单
  - 重算时已补偿项把补偿流水计入账侧，差异必然收敛为 0，不重复产生差异单 / 补偿；重核已自行消失的待处理差异自动核销
  - 历史业务日可随时补核（种子含已核平的历史任务单与跨日在途冻结单）
- **同步**：补偿后余额、`remain/frozen`、运营看板（待复核差异/已对账日/补偿笔数与净额）、操作记录实时联动；导航栏对待复核差异显示角标
- 逻辑冒烟测试：`npm run test:recon`（覆盖核平 / 差异生成 / 补偿收敛 / 幂等重跑 / 库存与预占修正 / 挂账 / 跨日补偿归属 / 看板审计同步 / 角色权限）；`npm test` 同时运行两套测试

## 后续可扩展

- 风控规则增强（设备指纹、IP/地域聚类、行为模型评分；申诉证据图片上传）
- 活动人群黑白名单、地区/标签定向
- 多种抽奖玩法（盲盒、九宫格、开红包）
- 奖品发放流程对接（发货单、快递单号）
- 数据埋点与 AB 测试、报表导出
- 真实后端 + 数据库持久化、多租户与细粒度 RBAC 权限